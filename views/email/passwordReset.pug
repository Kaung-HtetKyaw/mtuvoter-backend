extends baseEmail

block content
  p Hi #{firstName},
  p Forgot your password? Reset your password with the link below.
  p (Website for this action not yet implememnted.)
  p TOKEN: #{url}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  - const token = url.split('/resetPassword/')[1]
                  - const baseUrl = url.split('/api')[0];
                  a(href=`${baseUrl}/resetPassword/${token}`, target='_blank') Reset your password
  p If you didn't forget your password, please ignore this email!